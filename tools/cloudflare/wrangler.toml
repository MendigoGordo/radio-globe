name = "radio-globe-api"
main = "worker.js"
compatibility_date = "2024-11-01"

# Sem bindings/segredos: o Worker so faz proxy+cache de uma API publica.
# Apos o deploy, a URL publica sera algo como:
#   https://radio-globe-api.SEU-SUBDOMINIO.workers.dev
# Cole essa URL em data/config.js (window.RADIO_GLOBE_API_PROXY).
